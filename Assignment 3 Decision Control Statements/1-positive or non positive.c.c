//to check whether a given number is positive or non positive
#include<stdio.h>
int main()
{
	int num;
	printf("Enter any number: ");
	scanf("%d",&num);
	 if(num>=0)
	 {
	 	printf("Number is positive");
	 }
	 else
	 {
	 	printf("Number is  non positive");
	 }
	return 0;
}
