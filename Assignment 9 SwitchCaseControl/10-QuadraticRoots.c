//C program to find all roots of a quadratic equation using switch case
#include<stdio.h>
int main()
{
	int a,b,c;
	int x1,x2;
	 
	switch((b*b)-4*(a*c)>=0)
	{
		case 0:
			switch((b*b)-4(*a*c)<0)
			{
				case 1:
					printf("Imaginary solution:");
					break;
			}
		case 1:
			switch((b*b)-4(*a*c)>0)
			{
				case 0:
					printf("One solution is: %d",x1);
					break;
				case 1:
					x=(-b+)
				   printf("Two real solution is: %d %d",x1,x2);
				   break;	
			}
	}
	}
	return 0;
}
